#include <iostream>
#include <vector>
using namespace std;

class Solution {
public:
    int maxArea(vector<int>& height) {

    }
};

int main() {
    Solution solution;
    vector<int> height;
    height.push_back(4);
    height.push_back(3);
    height.push_back(2);
    height.push_back(1);
    height.push_back(4);
    solution.maxArea(height);
    return 0;
}
